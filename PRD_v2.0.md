# 「译语」AI 翻译输入助手 · PRD v2.0

> 代号：**LinguaFlow / 译语**
> 定位：全平台、BYOK、隐私优先的 AI 语音翻译键盘

| 项 | 内容 |
|---|---|
| **版本** | v2.0 |
| **日期** | 2026-09-26 |
| **文档范围** | 仅产品需求。实施计划见 [`roadmap.md`](./roadmap.md)，关键决策见 [`decisions/`](./decisions/)，下一步选项见 [`next_steps.md`](./next_steps.md) |
| **原始素材** | `assets/PRD_v2.0_source.docx` |

**目录**

1. [产品概述](#1-产品概述)
2. [核心使用流程](#2-核心使用流程)
3. [功能清单与优先级](#3-功能清单与优先级)
4. [交互与热键设计](#4-交互与热键设计)
5. [设计风格规范](#5-设计风格规范)
6. [页面清单](#6-页面清单)
7. [异常与边界](#7-异常与边界)
8. [非功能需求](#8-非功能需求)
9. [技术架构概要](#9-技术架构概要)

---

## 1. 产品概述

| 项 | 内容 |
|---|---|
| **一句话定位** | 按住说话（或打一句话），译文自动落进你正在输入的地方——用你自己的模型 Key，离线也能用 |
| **目标用户** | 跨语言办公/学习者、出海从业者、跨境电商客服、多语开发者、隐私敏感人群 |
| **三大支柱** | ① BYOK：任意模型平台 Key 自由配置 ② 五端全覆盖（含 Linux）+ 本地离线模型 ③ 数据不出本机、流量直连模型方 |
| **对标** | Chatterfly（语音成稿/国内）、Typeless（语音键盘/海外）、Bob/Pot（划词翻译） |

### 1.1 竞争格局与差异点

| 维度 | Typeless | Chatterfly | Bob | **译语（本产品）** |
|---|---|---|---|---|
| 核心形态 | 语音键盘 → 润色成稿 | 语音输入 → 混元润色 | 划词 / 截图翻译 | **语音 + 文本翻译输入** |
| 平台 | macOS / Windows / iOS / Android | macOS / Windows（移动端未发布） | 仅 macOS | **五端全覆盖（含 Linux）** |
| BYOK | ❌ | ❌ | 部分插件 | **✅ 任意平台** |
| 离线 / 本地模型 | ❌ 纯云端 | ❌ | ❌ | **✅ Ollama / 端侧** |
| 隐私 | 采集周边文本、云端处理 | 录音本地 | 本地 | **零采集、全本地** |
| 价格 | $12–30 / 月 | 免费 | 买断 ¥50 | **买断制为主** |

**定位一句话**：翻译版的 Typeless + 可自带钥匙的 Spokenly + 全平台。竞品以订阅锁定模型、平台与数据流向；译语三者均对用户开放。

**术语约定**：下文「五端」指 macOS / Windows / Linux / iOS / Android；「端侧」指运行在用户设备上的本地模型。

---

## 2. 核心使用流程

流程编号在本文档内全局引用：A 语音翻译输入、B 悬浮窗输入翻译、C 划词翻译、D 静默替换翻译、E 截图 OCR 翻译。

### 2.1 流程 A：按住说话，译文落框（语音翻译输入）

**场景**：用户在 WhatsApp / Telegram / 邮件里要给外国客户发消息。

```
① 光标停留在任意输入框
        ↓
② 按住 Fn（默认，可改）→ 屏幕底部浮出细录音条（波形动画 + 计时）
   直接说中文："那个报价我确认没问题，下周三之前可以签合同"
        ↓
③ 松开 Fn → 录音条变为"转写/翻译中"状态条（显示已输出字符数）
   · 语音转写（本地 Whisper 或用户配置的 STT API）
   · 自动去口头禅、补标点、按语境成稿
   · 翻译成目标语言，流式输出
        ↓
④ 状态条预览译文 1.2 秒（可点展开编辑），随后自动写入输入框并消失
   Esc 可随时取消
```

**关键规则**（吸收 Typeless / Chatterfly 已验证的交互）：

- 按住说话、松手成稿，全程用户不离开当前应用，无悬浮窗打断
- 预览期的 1.2 秒为"后悔窗口"，可在设置中改为"直接写入"或"总是预览"
- 长语音不设 6 分钟硬上限（Typeless 的已知痛点），仅受所用模型上下文限制
- 中途说错可继续说"不对，改成……"，成稿引擎只保留最终意图

### 2.2 流程 B：悬浮窗输入翻译（打字场景）

按唤起热键（默认 ⌥空格 / Alt+空格）→ 鼠标旁弹出迷你悬浮窗 → 直接打字或粘贴（唤起时自动读取剪贴板）→ 流式显示译文 → Enter 写回并关闭；⌘/Ctrl+Enter 仅复制。

### 2.3 流程 C：划词翻译（阅读场景）

选中任意应用文本 → 按 ⌥D / Ctrl+Alt+D → 选区旁浮出译文小窗 → 点空白关闭 / 点"替换"写回（可编辑区域）/ 点喇叭朗读。

### 2.4 流程 D：静默替换翻译（聊天场景，无窗口打断）

输入框敲好中文 → 按 ⌥回车 / Ctrl+Alt+回车 → 底部状态条显示进度（翻译中…已输出 N 字，可取消）→ 完成：输入框原文被译文替换；期间切换焦点则终止并 toast。

### 2.5 流程 E：截图 OCR 翻译

按 ⌥S / Ctrl+Alt+S → 框选区域 → 悬浮窗双栏「原文 | 译文」，均可复制。

### 2.6 移动端流程（iOS / Android）

**键盘模式**：唤出「译语键盘」→ 顶部工具条：语言方向（中→英，可点互换）· 翻译开关 · 🎤 → 打字中文 → 候选区流式输出英文译文 → 点上屏 → 长按 🌐 切回系统键盘。

**iOS 已知限制与对策**：

- 系统不允许自动切换键盘 → App 内提供"引导开启键盘 + 添加到常用列表"教程，并在键盘上放置显眼的大切换键以降低切换成本
- 键盘扩展内不能访问麦克风 → 语音翻译走「App 内录音 → 译语文本 → 分享 / 复制」流程，键盘内只做文本翻译
- 不占用 emoji 键位，译语键放在工具条左侧而非替换系统键

---

## 3. 功能清单与优先级

优先级定义：**P0** = macOS MVP 必须交付；**P1** = MVP 之后补完。

| 模块 | 功能 | 优先级 |
|---|---|---|
| 语音输入 | 按住说话 → 转写 → 润色 → 翻译 → 写回（全流程无窗口） | **P0** |
| 语音输入 | 本地 Whisper 转写（离线可用）+ 可选云端 STT（BYOK） | P1 |
| 翻译 | 悬浮窗输入翻译（流程 B）、划词（流程 C）、静默替换（流程 D） | **P0** |
| 翻译 | 截图 OCR 翻译（流程 E） | P1 |
| 翻译 | 语言自动检测、方向互换、流式渲染、双语写回 | **P0** |
| 翻译 | 术语表 / 自定义词典（注入 prompt，防专名错译） | **P0** |
| 翻译 | 风格选择（直译 / 意译 / 商务 / 口语）+ 纠错改口指令 | P1 |
| 模型 | BYOK：OpenAI 兼容协议 / Claude / Gemini / 自定义 BaseURL | **P0** |
| 模型 | 本地模型（Ollama / 端侧 MLC） | P1 |
| 模型 | 多配置 Profile、按场景路由（翻译用便宜的、润色用强的） | P1 |
| 效率 | Skill 场景模板（会议纪要 / 邮件 / 营销文案，可自定义分享） | P1 |
| 效率 | 历史记录、收藏、用量统计（token / 预估花费） | P1 |
| 系统 | 全局热键（可自定义、冲突检测）、隐私锁 | **P0** |
| 系统 | 开机自启、托盘 / 菜单栏常驻、断网提示（切换离线模式） | P1 |
| 平台 | 桌面三端（Windows / macOS / Linux） | **P0** |
| 平台 | Android 输入法 + iOS 键盘扩展 | P1 |

---

## 4. 交互与热键设计

**参照系**：ChatGPT 桌面版（修饰键 + 空格唤起）、Bob / 有道（Ctrl / ⌥ + Alt + 字母翻译动作）、Typeless / Chatterfly（Fn 按住说话）。

| 功能 | macOS | Windows / Linux | 参照 |
|---|---|---|---|
| 语音输入（按住说话） | 按住 Fn | 按住 Fn | Typeless / Chatterfly |
| 唤起悬浮窗 | ⌥ + 空格 | Alt + 空格 | ChatGPT 桌面版 |
| 划词翻译 | ⌥ + D | Ctrl + Alt + D | Bob / 有道 |
| 截图翻译 | ⌥ + S | Ctrl + Alt + S | Bob / 有道 |
| 静默替换翻译 | ⌥ + ↩ | Ctrl + Alt + ↩ | Bob 静默划词 |
| 打开主窗口 | 双击 ⌥ | 双击 Ctrl | 有道 |
| 临时禁用全部热键（演示 / 免打扰） | ⌥ + ⇧ + P | Ctrl + Alt + P | 自研 |

**交互规则**：

1. 全部可重绑，实时冲突检测（含微信截图 Ctrl+Alt+A 等常见占用提示）
2. 唤起键单次触发、语音键按住触发，两类手感必须区分
3. 流程 D 进行中若焦点变化，立即终止并 toast
4. 悬浮窗动画 ≤ 180ms（淡入 + 上移 8px），不抢焦点
5. 热键全局生效：macOS 需引导开启"辅助功能 + 输入监控"权限，首次启动走授权引导流程

---

## 5. 设计风格规范

### 5.1 基调

轻盈、无侵入、半透明质感——"伴随型"软件的存在感必须低于宿主应用。参考系：Chatterfly 的浅色圆角卡片 + 柔和蓝紫渐变、Bob 的紧凑悬浮窗、Typeless 的极简录音条。

- **圆角**：悬浮窗 14px / 卡片 10px / 按钮 8px / 输入框 10px
- **阴影**：悬浮窗 24% 黑投影（y8, blur24），营造浮于所有窗口之上的层级
- **尺寸**：
  - 迷你悬浮窗 380 × 自适应（min 120 / max 480px 滚动）
  - 划词小窗 320 × 自适应
  - 录音 / 状态条 280 × 32px，屏幕底部居中偏上 80px（细条形态，不打断视觉中心）
  - 主窗口 960 × 640（最小 720 × 480）
- **双主题**：跟随系统

### 5.2 Design Tokens

| Token | 浅色 | 深色 |
|---|---|---|
| 品牌主色 | #4F7CFF | #5B8CFF |
| 强调色（CTA / 发送） | #FF7A45 | #FF8A5C |
| 悬浮窗背景 | #FFFFFFEB（92%） | #1E1F24E0（88%） |
| 正文 / 次要文字 | #1D2129 / #86909C | #E8EAED / #9AA0A6 |
| 分割线 / 原文区底 | #F0F1F5 / #F7F8FA | #2E3038 / #26282E |
| 错误态 | #F53F3F | #F76965 |
| 字体 | PingFang SC / HarmonyOS Sans + Inter；译文区 15px | 同左 |

### 5.3 核心组件规格

**① 录音 / 状态条**（最高频组件，三态）

```
[◉●●● 波形]  00:07                              ← 按住说话态：主色波形动画 + 计时
[✕] 翻译中… 已输出 32 字  →  ←                    ← 处理态：可点展开、可取消、显示字数
[✓] "Confirmed, we can sign by Wednesday."      ← 预览态 1.2s 后自动写入
```

**② 迷你悬浮窗**

```
┌──────────────────────────────────────────┐
│ [🌐 中 → 英 ▾]              [🔊] [⚙] [✕]  │
│┌────────────────────────────────────────┐  │
││ 原文（浅灰底，可折叠）                  │  │
│└────────────────────────────────────────┘  │
│┌────────────────────────────────────────┐  │
││ 译文流式输出区（白底，顶部 2px 进度条）  │  │
│└────────────────────────────────────────┘  │
│ [🎤] [输入消息…                    ] [➤发送] │
└──────────────────────────────────────────┘
```

空输入时发送键置灰；译文区浮动操作：复制 / 双语写回 / 收藏 / 换 Profile 重译。

**③ 划词小窗**：原文 12px 次要色在上、译文 15px 在下，底部图标排（复制 / 替换 / 朗读 / 收藏 / 进主窗）。

**④ 设置页（主窗内，左侧导航）**：模型配置 / 快捷键 / 翻译偏好 / 术语表 / Skills / 历史与用量 / 隐私安全。**模型配置为灵魂页面**：平台卡片网格（OpenAI、Anthropic、Gemini、DeepSeek、通义、火山、Ollama、自定义 OpenAI 兼容）→ BaseURL + Key + 模型下拉（支持拉取列表）+ 测试连接。

**⑤ 移动端键盘**：与系统键盘同高；顶部 36px 工具条（语言方向、翻译开关、🎤、大切换键）；候选区为"译文流"。

**⑥ 首次引导三步**：选平台 → 填 Key（附各平台申请链接，支持"先用本地模型试用"零门槛体验）→ 设热键。无强制登录。

---

## 6. 页面清单（原型图清单）

| # | 页面 | 平台 | 状态要求 |
|---|---|---|---|
| 1 | 录音条（说话 / 处理 / 预览三态） | 桌面 | 核心组件，三态都要画 |
| 2 | 迷你悬浮窗（空 / 加载 / 流式 / 失败） | 桌面 | 四态 |
| 3 | 划词译文小窗 | 桌面 | |
| 4 | 静默状态条 + 焦点变化 toast | 桌面 | |
| 5 | 截图 OCR 双栏窗 | 桌面 | |
| 6 | 主窗口-首页（历史 + 用量统计） | 桌面 | |
| 7 | 模型配置（列表 / 新增 / 测试连接） | 全端 | |
| 8 | 快捷键设置（冲突检测态） | 桌面 | |
| 9 | 翻译偏好 / 术语表 / Skills | 全端 | |
| 10 | 首次引导三步 + 权限引导 | 全端 | |
| 11 | 移动端 App 主页 + 隐私锁 | 移动 | |
| 12 | 移动端键盘（工具条 / 候选流） | 移动 | |
| 13 | 托盘 / 菜单栏菜单 | 桌面 | |

---

## 7. 异常与边界

| 场景 | 处理 |
|---|---|
| Key 无效 / 欠费 / 限流 | 内联错误条显示模型方返回信息 + 直达配置页 |
| 断网 | 提示切换"离线模式"（本地 Whisper + 本地 LLM，若已配置） |
| 流程 D 进行中焦点切换 | 终止任务，toast 提示，原文不动 |
| 未配置任何模型 | 默认引导"本地模型一键体验"，绝不白屏 |
| 隐私锁开启 | 主窗需 FaceID / 密码；悬浮窗功能不受影响 |
| 热键被占用 | 设置页红字提示占用方 |
| iOS 键盘未启用 | App 检测后弹引导，一键跳系统设置 |

---

## 8. 非功能需求

- **性能**：热键到窗口可见 < 150ms；首 token < 1.5s（主流 API）；录音条波形 60fps
- **隐私**：零遥测默认；请求只发往用户配置的 BaseURL；不采集宿主应用任何上下文文本（写入隐私政策）
- **稳定性**：热键监听崩溃自愈；更新不丢配置
- **国际化**：界面中 / 英双语

---

## 9. 技术架构（概要）

**Flutter（UI + 业务）+ 原生桥（热键 / 剪贴板 / 焦点 / IME / 麦克风 / 钥匙串）+ Provider 适配层（OpenAI 兼容 / Anthropic / Gemini / 自定义 / Ollama 本地）+ SQLite + 系统密钥存储**；流量直连模型方，无自有中转服务器。

分层设计与 Provider 接口草案见 [`decisions/004-flutter-native-bridge.md`](./decisions/004-flutter-native-bridge.md)。

---

## 关联文档

| 文档 | 内容 |
|---|---|
| [`roadmap.md`](./roadmap.md) | 实施路线图、工期估算、里程碑与项目风险 |
| [`decisions/`](./decisions/) | ADR-001 BYOK、ADR-002 买断定价、ADR-003 五端覆盖、ADR-004 Flutter 技术栈 |
| [`competitors/`](./competitors/) | 竞品分析：Typeless / Chatterfly / Bob |
| [`next_steps.md`](./next_steps.md) | 下一步选项决策记录 |
