# 会话回执 · 2026-09-26-01-prd-reorg

- **任务 ID**：T-001
- **目标**：把 PRD v2.0 整理为纯产品需求文档，路线图/商业/下一步类内容移出
- **结果**：✅ 完成

## 做了什么

- `PRD_v2.0.md` 重写：保留 §1 产品概述、§2 流程 A–E 与移动端、§3 功能清单与优先级、§4 交互与热键、§5 设计风格规范、§6 页面清单、§7 异常与边界、§8 非功能需求、§9 技术架构概要
- 删除原 §10 商业模式、§11 风险与对策、文末"下一步（Kimi 三步选项）"，改为文末「关联文档」索引
- 新增头部元信息表（版本 / 日期 / 文档范围）+ 目录
- 术语统一：五端（macOS/Windows/Linux/iOS/Android）、端侧；表格分隔符统一；优先级拆行（原"P0 / OCR 为 P1"混在一格）
- 去掉口语化表述（如"这是最好写进官网的对比表"）
- 同步：`README.md`（11 节 → 9 节）、`INTERNAL.md`（文件结构描述）、`decisions/001`（PRD §10 → ADR-002）、`decisions/002`（承接商业模式，标注为定价唯一来源）、`next_steps.md`（来源说明）

## 关键决策与理由

- **保留 §1–§9 编号**：`roadmap.md` 与 ADR 中存在大量 `PRD §2.6 / §3 / §5.3` 引用，重排编号会导致全仓引用失效
- **商业模式下沉 ADR-002** 而非删除：定价是已决策内容（ADR-002），原文已有完整论证，只把 PRD 里的重复描述移除
- **风险章节不保留**：`roadmap.md` 已有项目风险表，PRD 只留产品级的"异常与边界"

## 遗留 / 下一步

- T-005：next_steps 三选项仍未拍板（需用户）
- T-007：竞品数据仍是二手调研，未实测

## 给下一个 Agent 的提示

- 沙箱内 `git push` 若报 TLS 握手失败，是 DNS 被劫持到内网 IP，把 GitHub 真实 IP 写进 `/etc/hosts` 可恢复
- 该仓库为公开仓库，README 面向外部访客；内部进度写在 `docs/CONTEXT.md`
